[project]
name = "django-catalog"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Petro", email = "petrotymchuck@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "django>=6.1",
    "psycopg[binary]>=3.3.4",
    "python-dotenv>=1.2.3",
]

[project.scripts]
django-catalog = "django_catalog:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
